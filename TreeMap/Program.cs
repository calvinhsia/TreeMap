using System;
using Avalonia;

namespace TreeMap;

class Program
{
    // Entry point when running the Avalonia UI.
    public static void Main(string[] args)
    {
        AvaloniaProgram.BuildAvaloniaApp().StartWithClassicDesktopLifetime(args);
    }
}
